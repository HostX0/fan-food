# Fan Food — جاهز للنشر من GitHub إلى Vercel

هذا مشروع Next.js حقيقي بواجهة عربية وسلة وطلب عبر واتساب، وليس ملف المعاينة المستقل.

## النشر

1. فك ضغط النسخة وارفع **المحتويات** إلى جذر مستودع GitHub؛ يجب أن يظهر `package.json` مباشرة.
2. استورد المستودع في Vercel واضغط Deploy. إعدادات Next.js والبناء ومجلد الناتج محفوظة في `vercel.json` وإصدار Node محفوظ في `package.json`.
3. لا تحتاج قاعدة بيانات، متغيرات بيئة، مفاتيح API أو أوامر على جهازك.

إذا ربطت المستودع الحالي `HostX0/fan-food`، يتم نشر تغييرات `main` تلقائياً بواسطة تكامل GitHub الموجود في Vercel. لا تعد رفع الملفات لنفس المشروع لمجرد الحصول على تحديث منشور.

## البناء القابل لإعادة الإنتاج

`package-lock.json` يقفل اعتماديات الإنتاج؛ استخدم `npm ci` بدلاً من تغيير الإصدارات يدوياً.

```sh
npm ci
npm run typecheck
npm test
npm run build
npm start
```

نجح تنزيل الاعتماديات وفحص TypeScript واختبارات الطلب والبناء الفعلي للإنتاج بتاريخ 2026-09-17 في:
https://github.com/HostX0/fan-food/actions/runs/35256419903

ملاحظات المعاينة القديمة في README وdocs تصف النسخة قبل هذا التحقق. المرجع الأحدث لنتائج البناء وفحوصات المتصفح هو تبويب Actions، مع تقارير JSON ولقطات شاشة مرفقة بنتيجة كل تشغيل.

## التعديل

- الأصناف والأحجام والأسعار: `src/data/menu.json`
- رقم واتساب ورابط The Third Act: `src/data/site.ts`
- التنسيق: `src/app/globals.css`
- الصور: `public/images/`

## حدود الطلب

السلة تبقى في متصفح الزبون. بيانات التوصيل لا تُخزن في قاعدة بيانات. زر الطلب يفتح رسالة واتساب جاهزة؛ الزبون يضغط إرسال داخل واتساب والمطعم يؤكد التوفر والتوصيل والمبلغ النهائي. لا تتم إضافة رسوم توصيل غير معروفة. فحوصات المتصفح لا ترسل أي طلب حقيقي.
